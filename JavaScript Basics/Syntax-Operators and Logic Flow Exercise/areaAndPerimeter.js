//Write a JS function that calculates
// the area and perimeter of a rectangle by given two sides.

function findAreaAndPerimeter(sideA , sideB) {
    let area = sideA*sideB;
    let perimeter = sideA*2 + sideB*2
    console.log(area)
    console.log(perimeter)
}